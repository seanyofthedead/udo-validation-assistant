// UDO Detail + AI Findings — SPEC §7.4. Implemented in IMPLEMENTATION_PLAN 3.3.

export function Detail() {
  return (
    <section aria-labelledby="detail-title">
      <h2 id="detail-title">UDO Detail</h2>
      <p>Record, evidence, verdict, confidence, and justification appear here.</p>
    </section>
  );
}
