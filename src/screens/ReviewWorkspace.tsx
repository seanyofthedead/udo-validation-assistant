// Review Workspace — SPEC §7.5. Implemented in IMPLEMENTATION_PLAN 3.5.

export function ReviewWorkspace() {
  return (
    <section aria-labelledby="review-title">
      <h2 id="review-title">Review Workspace</h2>
      <p>Confirm or override verdicts, with disposition history, here.</p>
    </section>
  );
}
